import { getResources, getStatus, resourceReducer, actionTypes } from 'redux-resource';
import { Fetch } from 'react-request';
import Resource from './resource';

export {
  Resource,
  Fetch,
  getResources,
  getStatus,
  resourceReducer,
  actionTypes
};
